<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>  <!-- rebuild -->

    <parent>
        <groupId>com.sparkutils</groupId>
        <artifactId>dmn-4-spark_root_${dbrCompatVersion}${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>dmn-4-spark_${dbrCompatVersion}${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
    <name>DMN 4 Spark Server</name>
    <description>DMN 4 Spark backend implementation</description>

    <profiles>
        <profile>
            <id>Spark4</id>
            <dependencies>
                <!-- actual client code needed to connect is passed via spark.api -->
                <!-- shared test code - Probably shouldn't need it -->
                <dependency>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect-client-jvm_${scalaCompatVersion}</artifactId>
                    <version>${sparkVersion}</version>
                    <classifier>tests</classifier>
                    <scope>test</scope>
                </dependency>
                <!-- actual server to spawn for testing -->
                <dependency>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect_${scalaCompatVersion}</artifactId>
                    <version>${sparkVersion}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>Spark41</id>
            <dependencies>
                <!-- actual client code needed to connect is passed via spark.api -->
                <!-- shared test code - Probably shouldn't need it -->
                <dependency>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect-client-jvm_${scalaCompatVersion}</artifactId>
                    <version>${sparkVersion}</version>
                    <classifier>tests</classifier>
                    <scope>test</scope>
                </dependency>
                <!-- actual server to spawn for testing -->
                <dependency>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect_${scalaCompatVersion}</artifactId>
                    <version>${sparkVersion}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>Spark42</id>
            <dependencies>
                <!-- actual client code needed to connect is passed via spark.api -->
                <!-- shared test code - Probably shouldn't need it -->
                <dependency>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect-client-jvm_${scalaCompatVersion}</artifactId>
                    <version>${sparkVersion}</version>
                    <classifier>tests</classifier>
                    <scope>test</scope>
                </dependency>
                <!-- actual server to spawn for testing -->
                <dependency>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect_${scalaCompatVersion}</artifactId>
                    <version>${sparkVersion}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <dependencies>
        <dependency>
            <groupId>com.sparkutils</groupId>
            <artifactId>shim_compilation_${shimCompilationRuntime}_${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
            <version>${shimCompilationVersion}</version>
            <scope>provided</scope>
            <classifier>sources</classifier>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-sql_${scalaCompatVersion}</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.sparkutils</groupId>
            <artifactId>shim_runtime_${shimRuntime}_${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
            <version>${shimRuntimeVersion}</version>
        </dependency>

        <dependency>
            <groupId>com.sparkutils</groupId>
            <artifactId>testing-runtime_${dbrCompatVersion}${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
            <version>${testingVersion}</version>
        </dependency>

        <dependency>
            <groupId>com.sparkutils.</groupId>
            <artifactId>testing_${dbrCompatVersion}${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
            <version>${testingVersion}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.sparkutils</groupId>
            <artifactId>dmn-4-spark_api_${dbrCompatVersion}${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
            <version>${revision}</version>
            <exclusions>
                <exclusion>
                    <groupId>com.sparkutils</groupId>
                    <artifactId>dmn-4-spark_api_stub_${dbrCompatVersion}${sparkCompatVersion}_${scalaCompatVersion}</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_${scalaCompatVersion}</artifactId>
            <version>${sparkVersion}</version>
            <scope>provided</scope> 
        </dependency>

        <!-- databricks runtime version is chosen to ease deployment there -->
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.scalatest</groupId>
                <artifactId>scalatest-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
